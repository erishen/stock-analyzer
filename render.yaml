services:
  - type: web
    name: stock-analyzer-demo
    runtime: python
    repo: https://github.com/erishen/stock-analyzer
    plan: free
    buildCommand: pip install -U uv && uv sync --extra web --reinstall-package stock-analyzer --no-dev --no-cache && cd frontend && npm install --registry=https://registry.npmmirror.com && npm run build && cd .. && STOCK_ANALYSIS_DB_PATH=/opt/render/project/src/data/stock_analysis.db uv run python scripts/seed_demo.py
    startCommand: uv run stock-analyzer web --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/stats
    envVars:
      - key: STOCK_ANALYSIS_DB_PATH
        value: /opt/render/project/src/data/stock_analysis.db
      - key: AGENT_CHAT_PUBLIC
        value: "1"
